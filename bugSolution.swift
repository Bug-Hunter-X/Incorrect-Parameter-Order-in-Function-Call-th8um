func calculateArea(width: Double, height: Double) -> Double {
    return width * height
}

let area = calculateArea(width: 10, height: 5) // Correct usage

//Always pass parameters in the correct order to avoid errors.
let correctArea = calculateArea(width: 10, height: 5) // Correct usage